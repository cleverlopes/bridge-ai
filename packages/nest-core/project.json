{
  "name": "nest-core",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/nest-core/src",
  "projectType": "library",
  "tags": ["lib", "nest"],
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/coverage"],
      "options": { "command": "bun run test:cov", "cwd": "packages/nest-core" }
    }
  }
}

---
phase: 8.5-workspace-onboarding
plan: 02
subsystem: workspace
tags: [nestjs, injectable, fs, indexing, git, tree-walk, manifest-detection]

# Dependency graph
requires:
  - phase: 8.5-01
    provides: WorkspaceModule skeleton, types.ts (RepoInfo, IndexPayload interfaces)
provides:
  - RepoIndexerService with bootstrap() method for full repo indexing
  - Tree walk with depth=3 and 2000-entry cap with truncated flag
  - Manifest detection for npm, python, rust, go, maven, gradle
  - Entrypoint detection for main.ts, index.ts, app.ts variants
  - Test path detection for *.spec.ts, *.test.ts, test/, __tests__/ patterns
  - Doc path detection for README.md, CHANGELOG.md, docs/, .planning/ patterns
affects: [8.5-01, 8.5-03, 8.5-04]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "TDD: failing test commit before implementation commit"
    - "Jest mock of node:fs/promises using jest.mock() at module level"
    - "Static readonly class constants for patterns and limits"
    - "Async tree walk with early exit on entry limit"

key-files:
  created:
    - packages/nest-core/src/module/workspace/types.ts
    - packages/nest-core/src/module/workspace/repo-indexer.service.ts
    - packages/nest-core/src/module/workspace/__tests__/repo-indexer.service.spec.ts
    - packages/nest-core/src/module/workspace/workspace.module.ts
  modified: []

key-decisions:
  - "types.ts created as part of 8.5-02 since 8.5-01 had not run in this parallel agent context"
  - "workspace.module.ts created minimal (no KsmModule/TypeORM since those come from 8.5-01)"
  - "ENTRYPOINT_PATTERNS uses a Set for O(1) lookup against known basename patterns"
  - "walkTree skips hidden directories except .planning to avoid indexing .git/ etc."
  - "Manifest content: npm (package.json) parsed as JSON; others stored as { raw: string }"
  - "readFile mock uses jest.Mock type cast to avoid TypeScript overload ambiguity in tests"

patterns-established:
  - "Pattern 1: node:fs/promises readdir called with { withFileTypes: true } for Dirent entries to distinguish files from dirs"
  - "Pattern 2: Tree walk returns boolean (truncated flag) and mutates results array by reference for efficiency"
  - "Pattern 3: Detection methods all operate on in-memory string[] tree, no additional fs calls except detectManifests which reads content"

requirements-completed: [ONBOARD-04, ONBOARD-06]

# Metrics
duration: 8min
completed: 2026-04-01
---

# Phase 8.5 Plan 02: Repo Indexer Service Summary

**RepoIndexerService.bootstrap() indexing tree walk (depth=3, cap=2000), manifest detection (npm/python/rust/go/maven/gradle), entrypoint/test/doc path detection — 10 tests passing**

## Performance

- **Duration:** 8 min
- **Started:** 2026-04-01T20:19:29Z
- **Completed:** 2026-04-01T20:27:00Z
- **Tasks:** 1 (TDD: RED + GREEN)
- **Files modified:** 4

## Accomplishments
- RepoIndexerService with full bootstrap() implementation: tree walk, manifest detection, entrypoint/test/doc detection
- TDD approach: RED commit (failing test for missing module) then GREEN commit (all 10 tests pass)
- Tree depth-limited to 3 levels, count-limited to 2000 entries with `truncated` flag on overflow
- Types.ts and WorkspaceModule skeleton created as required Plan 01 dependencies (parallel execution context)

## Task Commits

1. **RED: test(8.5-02): add failing tests for RepoIndexerService** - `7214a97` (test)
2. **GREEN: feat(8.5-02): implement RepoIndexerService with full test coverage** - `a9f29a0` (feat)

## Files Created/Modified
- `packages/nest-core/src/module/workspace/types.ts` - RepoInfo, IndexPayload, ManifestEntry, InitWorkspaceDto interfaces (from plan 01 spec)
- `packages/nest-core/src/module/workspace/workspace.module.ts` - WorkspaceModule NestJS module with RepoIndexerService in providers+exports
- `packages/nest-core/src/module/workspace/__tests__/repo-indexer.service.spec.ts` - 10 unit tests covering all detection behaviors
- `packages/nest-core/src/module/workspace/repo-indexer.service.ts` - Full RepoIndexerService implementation

## Decisions Made
- types.ts and workspace.module.ts skeleton created in this plan (8.5-02) because the parallel agent context did not have 8.5-01 completed yet. The content matches the 8.5-01 spec exactly so there is no conflict.
- workspace.module.ts kept minimal (no TypeORM or KsmModule) since those come from 8.5-01 which handles WorkspaceSnapshot entity and WorkspaceOnboardingService. The two modules will be merged when 8.5-01 runs.
- `readFile` mock cast to `jest.Mock` (not the typed overload version) to avoid TypeScript failing on overload resolution in tests.
- Hidden directories prefixed with `.` are skipped during tree walk (except `.planning` which is explicitly allowed as a doc dir).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Created types.ts and workspace.module.ts as Plan 01 dependencies were missing**
- **Found during:** Task 1 setup
- **Issue:** Plan 02 requires types.ts (IndexPayload, RepoInfo) and workspace.module.ts from Plan 01, but Plan 01 had not run in this parallel agent's git branch
- **Fix:** Created types.ts and workspace.module.ts following the exact spec from 8.5-01-PLAN.md; workspace.module.ts is minimal (providers/exports for RepoIndexerService only) as the full module wiring is Plan 01's responsibility
- **Files modified:** packages/nest-core/src/module/workspace/types.ts, packages/nest-core/src/module/workspace/workspace.module.ts
- **Verification:** TypeScript compiled without errors in workspace module; tests pass
- **Committed in:** 7214a97

---

**Total deviations:** 1 auto-fixed (1 blocking dependency)
**Impact on plan:** Required workaround for parallel execution — types.ts and workspace.module.ts skeleton created to unblock this plan. No scope creep; content matches Plan 01 spec exactly.

## Issues Encountered
- TypeScript overload ambiguity when casting `readFile` mock return value with `as unknown as Buffer` — resolved by using `jest.Mock` cast type instead of the strongly-typed overloaded function type.

## Known Stubs
None — all implementation is wired and functional.

## User Setup Required
None — no external service configuration required.

## Next Phase Readiness
- RepoIndexerService.bootstrap() is complete and ready to be consumed by WorkspaceOnboardingService.initWorkspace() (Plan 01 or Plan 03)
- Tree cap, manifest detection, entrypoint/test/doc detection all verified with tests
- Plan 01 (WorkspaceOnboardingService) should import and use RepoIndexerService.bootstrap() for ONBOARD-06

---
*Phase: 8.5-workspace-onboarding*
*Completed: 2026-04-01*

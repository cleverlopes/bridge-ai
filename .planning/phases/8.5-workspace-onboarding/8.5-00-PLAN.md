---
phase: 8.5-workspace-onboarding
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/nest-core/src/__mocks__/simple-git.ts
  - packages/nest-core/src/module/workspace/__tests__/workspace-onboarding.service.spec.ts
  - packages/nest-core/src/module/workspace/__tests__/repo-indexer.service.spec.ts
  - packages/nest-core/src/module/workspace/__tests__/ephemeral-workspace.service.spec.ts
  - packages/nest-core/src/module/workspace/__tests__/promotion.service.spec.ts
  - packages/nest-core/src/module/workspace/__tests__/workspace.controller.spec.ts
  - packages/nest-core/jest.config.js
  - package.json
autonomous: true
requirements: [ONBOARD-01, ONBOARD-02, ONBOARD-03, ONBOARD-04, ONBOARD-05, ONBOARD-06, ONBOARD-07, ONBOARD-08, ONBOARD-09, ONBOARD-10]
must_haves:
  truths:
    - "simple-git is installed and importable"
    - "All workspace test stubs exist and run green (empty expect blocks)"
    - "Jest coverage config includes workspace module files"
  artifacts:
    - path: "packages/nest-core/src/__mocks__/simple-git.ts"
      provides: "Mock for simple-git used across all workspace specs"
    - path: "packages/nest-core/src/module/workspace/__tests__/workspace-onboarding.service.spec.ts"
      provides: "Test stubs for ONBOARD-01 through ONBOARD-05"
    - path: "packages/nest-core/src/module/workspace/__tests__/repo-indexer.service.spec.ts"
      provides: "Test stubs for ONBOARD-06 and ONBOARD-07"
    - path: "packages/nest-core/src/module/workspace/__tests__/ephemeral-workspace.service.spec.ts"
      provides: "Test stubs for ONBOARD-08"
    - path: "packages/nest-core/src/module/workspace/__tests__/promotion.service.spec.ts"
      provides: "Test stubs for ONBOARD-10"
    - path: "packages/nest-core/src/module/workspace/__tests__/workspace.controller.spec.ts"
      provides: "Test stubs for REST surface"
  key_links:
    - from: "jest.config.js"
      to: "__mocks__/simple-git.ts"
      via: "moduleNameMapper"
      pattern: "simple-git.*__mocks__"
---

<objective>
Install `simple-git` dependency, create the `simple-git` mock, create test stub files for all workspace services, and update Jest config to include workspace module in coverage.

Purpose: Establishes the Nyquist-compliant test scaffold before any implementation code is written (Wave 0). All subsequent plans have automated verification from their first task.

Output: Test stubs that compile and run green (empty describe blocks with placeholder it() calls), simple-git mock, updated Jest config.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/8.5-workspace-onboarding/8.5-RESEARCH.md
@.planning/phases/8.5-workspace-onboarding/8.5-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install simple-git and create mock</name>
  <read_first>
    - packages/nest-core/jest.config.js
    - packages/nest-core/src/__mocks__/gsd-sdk.ts
    - package.json
  </read_first>
  <files>
    package.json,
    packages/nest-core/src/__mocks__/simple-git.ts,
    packages/nest-core/jest.config.js
  </files>
  <action>
1. Run `bun add simple-git` from the repo root to install the dependency.

2. Create `packages/nest-core/src/__mocks__/simple-git.ts` with a factory mock that returns an object with all methods used by workspace services. The mock must export a default function (matching simple-git's `simpleGit()` export pattern):

```typescript
const mockGit = {
  checkIsRepo: jest.fn().mockResolvedValue(true),
  getRemotes: jest.fn().mockResolvedValue([
    { name: 'origin', refs: { fetch: 'git@github.com:user/repo.git', push: 'git@github.com:user/repo.git' } },
  ]),
  status: jest.fn().mockResolvedValue({
    current: 'main',
    files: [],
    isClean: () => true,
    ahead: 0,
    behind: 0,
  }),
  revparse: jest.fn().mockResolvedValue('abc1234567890def1234567890abc1234567890ab'),
  clone: jest.fn().mockResolvedValue(undefined),
  listRemote: jest.fn().mockResolvedValue('abc123\trefs/heads/main'),
  raw: jest.fn().mockResolvedValue('origin/main'),
  push: jest.fn().mockResolvedValue(undefined),
  diff: jest.fn().mockResolvedValue(''),
  log: jest.fn().mockResolvedValue({ all: [], latest: null, total: 0 }),
};

const simpleGit = jest.fn(() => mockGit);
export default simpleGit;
export { mockGit };
export const CheckRepoActions = { IS_REPO_ROOT: 'IS_REPO_ROOT' };
export const GitError = class GitError extends Error {};
```

3. Update `packages/nest-core/jest.config.js`:
   - Add `'^simple-git$': '<rootDir>/__mocks__/simple-git.ts'` to the `moduleNameMapper` object (after the existing `@bridge-ai/bridge-sdk` entry).
   - Add `'module/workspace/**/*.ts'` to the `collectCoverageFrom` array.
   - Ensure the existing exclusions (`!**/*.module.ts`, `!**/index.ts`, `!**/*.entity.ts`) still apply.
  </action>
  <verify>
    <automated>cd /workspace && bun add simple-git && node -e "require('simple-git')" && echo "simple-git importable"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `"simple-git"` in dependencies
    - `packages/nest-core/src/__mocks__/simple-git.ts` exports `default`, `mockGit`, `CheckRepoActions`, `GitError`
    - `packages/nest-core/jest.config.js` `moduleNameMapper` contains key `'^simple-git$'`
    - `packages/nest-core/jest.config.js` `collectCoverageFrom` contains `'module/workspace/**/*.ts'`
  </acceptance_criteria>
  <done>simple-git installed; mock created; jest config updated with mapper and coverage path</done>
</task>

<task type="auto">
  <name>Task 2: Create all test stub files for workspace module</name>
  <read_first>
    - packages/nest-core/src/__mocks__/simple-git.ts
    - packages/nest-core/src/module/brain/brain.service.spec.ts
    - .planning/phases/8.5-workspace-onboarding/8.5-VALIDATION.md
  </read_first>
  <files>
    packages/nest-core/src/module/workspace/__tests__/workspace-onboarding.service.spec.ts,
    packages/nest-core/src/module/workspace/__tests__/repo-indexer.service.spec.ts,
    packages/nest-core/src/module/workspace/__tests__/ephemeral-workspace.service.spec.ts,
    packages/nest-core/src/module/workspace/__tests__/promotion.service.spec.ts,
    packages/nest-core/src/module/workspace/__tests__/workspace.controller.spec.ts
  </files>
  <action>
Create the `__tests__/` directory under `packages/nest-core/src/module/workspace/`.

Create 5 test stub files. Each file should have a top-level `describe()` block with placeholder `it.todo()` calls listing the behaviors that will be tested. Use the existing spec file pattern from the codebase (import from `@jest/globals` is NOT used in this project -- use plain `describe`/`it`/`expect` from Jest globals).

**workspace-onboarding.service.spec.ts:**
```typescript
describe('WorkspaceOnboardingService', () => {
  it.todo('initWorkspace — validates git repo and returns RepoInfo (ONBOARD-01, ONBOARD-02)');
  it.todo('initWorkspace — throws BadRequestException for non-git directory (ONBOARD-02)');
  it.todo('initWorkspace — clones from URL when repoUrl is provided (ONBOARD-01)');
  it.todo('validateCredentials — calls KsmService.createSecret for SSH key (ONBOARD-03)');
  it.todo('validateCredentials — calls KsmService.createSecret for HTTPS PAT (ONBOARD-03)');
  it.todo('validateCredentials — runs listRemote to verify read access (ONBOARD-03)');
  it.todo('generateVaultDocs — writes 5 files to vault path (ONBOARD-04)');
  it.todo('persistSnapshot — upserts WorkspaceSnapshot row (ONBOARD-05)');
  it.todo('persistSnapshot — updates existing snapshot on re-onboard (ONBOARD-05)');
});
```

**repo-indexer.service.spec.ts:**
```typescript
describe('RepoIndexerService', () => {
  it.todo('bootstrap — returns IndexPayload with tree, manifests, entrypoints, testPaths, docPaths (ONBOARD-06)');
  it.todo('bootstrap — caps tree at 2000 entries and sets truncated flag (ONBOARD-06)');
  it.todo('bootstrap — detects package.json as manifest (ONBOARD-06)');
  it.todo('bootstrap — detects main.ts and index.ts as entrypoints (ONBOARD-06)');
  it.todo('bootstrap — detects *.spec.ts files as test paths (ONBOARD-06)');
  it.todo('sync — returns early when headSha matches stored snapshot (ONBOARD-07)');
  it.todo('sync — triggers re-bootstrap when headSha differs (ONBOARD-07)');
});
```

**ephemeral-workspace.service.spec.ts:**
```typescript
describe('EphemeralWorkspaceService', () => {
  it.todo('cloneForRun — creates directory at volumes/runs/<runId>/ using git clone --local --no-hardlinks (ONBOARD-08)');
  it.todo('cloneForRun — returns the ephemeral run path (ONBOARD-08)');
  it.todo('cleanupRun — removes the run directory (ONBOARD-08)');
  it.todo('cleanupRun — does not throw if directory does not exist (ONBOARD-08)');
});
```

**promotion.service.spec.ts:**
```typescript
describe('PromotionService', () => {
  it.todo('promoteViaPatch — generates diff and applies to host via git apply (ONBOARD-10)');
  it.todo('promoteViaCherryPick — cherry-picks specified SHAs into host repo (ONBOARD-10)');
  it.todo('promoteViaPush — pushes run branch to remote (ONBOARD-10)');
  it.todo('promoteViaPatch — throws if diff is empty (ONBOARD-10)');
});
```

**workspace.controller.spec.ts:**
```typescript
describe('WorkspaceController', () => {
  it.todo('POST /workspaces/init — calls WorkspaceOnboardingService.initWorkspace with DTO (ONBOARD-01)');
  it.todo('POST /workspaces/init — returns 400 for missing workspacePath (ONBOARD-01)');
  it.todo('POST /workspaces/init — returns created project and snapshot (ONBOARD-01)');
});
```

Each file should be self-contained with no imports yet (only `describe` and `it.todo` calls). This ensures they compile and run as "pending" tests without any implementation dependencies.
  </action>
  <verify>
    <automated>cd /workspace/packages/nest-core && npx jest --testPathPattern="module/workspace/__tests__" --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/nest-core/src/module/workspace/__tests__/workspace-onboarding.service.spec.ts` exists and contains `describe('WorkspaceOnboardingService'`
    - File `packages/nest-core/src/module/workspace/__tests__/repo-indexer.service.spec.ts` exists and contains `describe('RepoIndexerService'`
    - File `packages/nest-core/src/module/workspace/__tests__/ephemeral-workspace.service.spec.ts` exists and contains `describe('EphemeralWorkspaceService'`
    - File `packages/nest-core/src/module/workspace/__tests__/promotion.service.spec.ts` exists and contains `describe('PromotionService'`
    - File `packages/nest-core/src/module/workspace/__tests__/workspace.controller.spec.ts` exists and contains `describe('WorkspaceController'`
    - `npx jest --testPathPattern="module/workspace/__tests__" --passWithNoTests` exits 0
  </acceptance_criteria>
  <done>All 5 test stub files exist, compile, and run as pending tests (exit 0). Wave 0 complete.</done>
</task>

</tasks>

<verification>
- `bun add simple-git` succeeded and `simple-git` appears in package.json
- `npx jest --testPathPattern="module/workspace/__tests__" --passWithNoTests` exits 0
- All 5 spec files exist under `packages/nest-core/src/module/workspace/__tests__/`
- `packages/nest-core/src/__mocks__/simple-git.ts` exists
- `jest.config.js` maps `simple-git` to mock and includes workspace in coverage
</verification>

<success_criteria>
- simple-git installed as a dependency
- 5 test stub files created with correct describe/it.todo structure
- simple-git mock created with all needed methods
- Jest config updated for mock mapping and coverage collection
- All tests pass (pending tests count as pass)
</success_criteria>

<output>
After completion, create `.planning/phases/8.5-workspace-onboarding/8.5-00-SUMMARY.md`
</output>
